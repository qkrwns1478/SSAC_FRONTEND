# CLAUDE.md — 프로세스 하네스

> 이 파일은 **Claude Code 전용** 프로세스 지침입니다.
> 환경 도구(lint · typecheck · scaffold 등)는 `AGENTS.md`를 참조하세요.
> 도구로 강제할 수 없는 **작업 절차 규칙**만 이 파일에 정의합니다.

---

## 🛡️ Agent Harness Protocols

| 프로토콜       | 파일 위치                             | 트리거 조건                              | 실행 방식           |
| -------------- | ------------------------------------- | ---------------------------------------- | ------------------- |
| SC 관심사 점검 | docs/agent-protocols/sc-harness.md    | SC가 포함된 작업 지시를 받았을 때        | 자동 (구현 전 필수) |
| 신규 기능 개발 | docs/agent-protocols/new-feature.md   | 새로운 컴포넌트/페이지/기능 추가 요청 시 | 자동 (구현 전 필수) |
| 테스트 작성    | docs/agent-protocols/testing.md       | 신규 컴포넌트/훅/API 연동 구현 완료 시   | 자동 (구현 후 필수) |
| 하네스 감사    | docs/agent-protocols/harness-audit.md | 하네스 점검 요청 시 / 주기적 실행        | 수동 또는 주기적    |
| 자가 진단      | docs/agent-protocols/self-diagnose.md | 에러 발생 / 구현 완료 후 검증 시         | 자동 (구현 후 필수) |
| ADR 생성       | docs/agent-protocols/adr-create.md    | 기술적 의사결정이 발생했을 때            | 수동 (결정 시점)    |

---

## 🔁 Protocol Trigger Rules

### ✅ 자동 실행 (에이전트가 반드시 실행해야 하는 프로토콜)

[sc-harness.md] 다음 키워드가 포함된 작업 지시를 받은 경우

- "SC", "Success Criteria", "성공 조건", "백로그"

[new-feature.md] 다음 요청을 받은 경우

- 새로운 페이지, 컴포넌트, API 연동, 기능 추가
- "만들어줘", "추가해줘", "구현해줘"

[testing.md] 다음 상황에서 실행

- 신규 컴포넌트, 페이지, 훅 구현 완료 시
- 신규 API 연동 코드 구현 완료 시
- 사용자가 "테스트 작성", "test", "검증" 언급 시
- quality.md TD-001 관련 작업 시

[self-diagnose.md] 다음 상황에서 실행

- 구현 완료 직후
- 에러 또는 예외 상황 발생 시
- 사용자가 "확인해줘", "점검해줘" 요청 시

### 🖐️ 수동 실행 (사용자 요청 또는 특정 시점에 실행)

[harness-audit.md] 다음 상황에서 실행

- 사용자가 "하네스 점검", "감사", "audit" 요청 시
- 스프린트 종료 시점

[adr-create.md] 다음 상황에서 실행

- 기술 스택, 아키텍처, 설계 방식에 대한 의사결정이 발생했을 때
- 사용자가 "기록해줘", "ADR", "의사결정" 언급 시

---

## ⚡ Protocol Execution Order

동시 트리거 발생 시 실행 순서:

1순위 sc-harness.md → SC 관심사 점검 (구현 전 게이트)
2순위 new-feature.md → 신규 기능 개발 프로토콜
3순위 testing.md → 테스트 작성 프로토콜
4순위 self-diagnose.md → 구현 후 자가 진단
5순위 adr-create.md → 의사결정 기록
6순위 harness-audit.md → 전체 하네스 감사

> 1순위 프로토콜 실행 결과가 중단(STOP)인 경우 이후 프로토콜은 실행되지 않는다.

---

## 🔍 Protocol Self-Check

작업 시작 전 에이전트는 반드시 아래 질문에 답해야 한다:

□ 이 작업에 SC가 포함되어 있는가?
→ YES : sc-harness.md 실행
□ 이 작업이 신규 기능 추가인가?
→ YES : new-feature.md 실행
□ 이 작업에서 기술적 의사결정이 발생했는가?
→ YES : adr-create.md 실행
□ 신규 컴포넌트/훅/API 연동 구현이 완료되었는가?
→ YES : testing.md 실행
□ 구현이 완료되었는가?
→ YES : self-diagnose.md 실행

위 질문을 건너뛰고 바로 구현을 시작하는 것은 금지된다.

---

## ⚠️ Protocol Conflict Resolution Rule

프로토콜 간 규칙이 충돌하는 경우 아래 우선순위를 따른다:

1순위 AGENTS.md → API 계약 규칙 (절대 우선)
2순위 CLAUDE.md → 전역 하네스 규칙
3순위 new-feature.md → 기능 개발 프로토콜
4순위 기타 프로토콜

충돌이 감지된 경우:
→ 임의로 판단하지 말고 충돌 내용을 출력한 후 작업을 중단하라
→ 출력 형식:
⚠️ CONFLICT DETECTED

- 충돌 규칙 A : [파일명] 내용
- 충돌 규칙 B : [파일명] 내용
  → 우선순위 규칙에 따라 [A/B]를 따릅니다.
  → 낮은 우선순위 규칙([B/A])은 이번 작업에서 건너뜁니다.

---

## 하네스 목록 (레거시 — 하위 호환용)

| 하네스                        | 트리거 조건                       | 위치                                                 |
| ----------------------------- | --------------------------------- | ---------------------------------------------------- |
| SC 관심사 점검 및 자동 재작성 | 작업 지시에 SC 항목이 포함된 경우 | [아래 참조](#1-sc-관심사-점검-및-자동-재작성-하네스) |

---

## 1. SC 관심사 점검 및 자동 재작성 하네스

**SC(Success Criteria)가 포함된 작업 지시를 받으면, 구현 전 반드시 Steps 1~4를 수행해 최종 SC를 확정한 뒤 Step 5에서 구현을 시작하세요.**

---

### Step 1. SC 항목 분류

아래 기준에 따라 입력된 모든 SC 항목을 분류하세요.

| 분류    | 판단 기준                                                                                                |
| ------- | -------------------------------------------------------------------------------------------------------- |
| ✅ FE   | UI 렌더링, 사용자 인터랙션, 라우팅, 페이지 이동, 상태 관리, 로딩/에러/빈 상태 처리, API 응답 데이터 표시 |
| ❌ BE   | 데이터 저장, 집계 로직, 필터링 기준 정의, 토큰 발급/무효화, 보안 정책, 권한 검증, DB 설계                |
| ⚠️ 혼재 | 하나의 항목에 FE와 BE 관심사가 함께 포함된 경우                                                          |

---

### Step 2. 점검 결과 출력

아래 형식으로 모든 SC 항목의 분류 결과를 출력하세요.

```
[분류] SC 항목 내용
→ 판정 이유 한 줄 요약
```

출력 예시:

```
✅ FE - 콘텐츠 목록 로딩 중 스켈레톤 UI가 표시되어야 한다
→ 로딩 상태에 대한 UI 처리로 FE 관심사에 해당

⚠️ 혼재 - 인기 콘텐츠 목록은 집계 기준(최근 7일)에 따라 정렬되어야 한다
→ 집계 기준 정의는 BE 관심사, 정렬된 데이터를 화면에 표시하는 것은 FE 관심사

❌ BE - Refresh Token 만료 시 서버에서 즉시 무효화되어야 한다
→ 서버 측 토큰 무효화 처리로 BE 관심사에 해당
```

---

### Step 3. FE 관점 자동 재작성

❌ BE 및 ⚠️ 혼재 항목을 아래 규칙에 따라 FE 관점으로 재작성하세요.

**재작성 규칙**

- BE 로직(집계, 저장, 검증 등)은 "API 응답을 통해 처리된다"는 전제로 제거하세요.
- FE가 수행해야 할 행위(표시, 이동, 처리, 노출 등)만 남기세요.
- 순수 BE 항목도 FE 관점으로 재작성하세요 (단순 제외가 아닌 관점 전환).

**재작성 출력 형식**

```
⚠️ 혼재 항목 재작성
- 원본: 인기 콘텐츠 목록은 집계 기준(최근 7일)에 따라 정렬되어야 한다
- 재작성: 인기 콘텐츠 목록은 API 응답 순서대로 화면에 노출되어야 한다
- 사유: 집계 및 정렬 기준은 BE 관심사이므로 제거, 응답 데이터를 그대로 표시하는 것으로 변경

❌ BE 항목 재작성
- 원본: Refresh Token 만료 시 서버에서 즉시 무효화되어야 한다
- 재작성: Refresh Token 만료 응답(401) 수신 시 로그인 페이지로 리다이렉트 되어야 한다
- 사유: 서버 측 무효화는 BE 관심사이므로 FE 응답 처리 관점으로 변경
```

---

### Step 4. 최종 SC 목록 확정 및 출력

재작성이 완료된 후, 아래 형식으로 최종 SC 목록을 출력하세요.

```
## 최종 Success Criteria (FE 구현 기준)
1. 항목 1
2. 항목 2
3. 항목 3
...

## 점검 요약
- 전체 SC 항목 수: N개
- ✅ FE 항목 (원본 유지): N개
- ⚠️ 혼재 항목 (재작성 완료): N개
- ❌ BE 항목 (FE 관점 재작성 완료): N개
```

---

### Step 5. 최종 SC 기반 구현 진행

최종 SC 목록을 기반으로 구현을 시작하세요.

**구현 시 항상 준수할 제약 사항**

- 데이터 집계, 필터링, 정렬 로직을 FE 코드에 직접 구현하지 마세요.
- 보안 정책 및 권한 검증 로직을 FE 코드에 포함하지 마세요.
- BE 관심사는 반드시 API 응답을 통해 처리하는 방식으로만 구현하세요.

---

<!-- 새 하네스 추가 시: 위 하네스 목록 테이블에 행을 추가하고, 동일한 ## N. 형식으로 절차를 작성하세요. -->
